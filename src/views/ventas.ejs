<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="estilos.css">
    <title>VentasPrueba</title>
</head>
<body>
        <div id="padre">
            <thead>
                    <div id="part1" class="parte1">
                        <p>Productos</p>
                        <select name="" id="">
                            <% if(typeof datos !== 'undefined'){ %>
                                <% for (index in datos){ %>
                                    <option><%= datos[index].Producto %></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                    <div id="part2">
                        <p>Precio</p>
                        <select type="number" name="Precio" id="Precio" class="ingreso">
                            <% if(typeof datos !== 'undefined'){ %>
                                <% for (index in datos){ %>
                                    <option><%= datos[index].Precio %> <b>Soles</b></option>
                                <% } %>
                            <% } %>
                        </select>
                    </div>
                <div id="part3">
                    <p>Cantidad</p>
                    <input type="number" id="cantidad" class="caja">  
                </div>
            </thead>
        </div>
    <button type="button" name="button" id="button" onclick="continuar()">continuar</button>
    <table id="info">
        <thead>
            <tr>
                <th>Producto</th>
                <th>Precio</th>
                <th>Cantidad</th>
                <th>SubTotal</th>
            </tr>
        </thead>
    </table>
    <div id="asd">
        <div>
            <p>SubTotal</p>
            <input type="text" id="SubTotal" class="caja">
        </div>
        <div>
            <p>IGV</p>
            <input type="text" id="IGV" class="caja">
        </div>
        <div>
            <p>Total:</p>
            <input type="text" id="Total" class="caja"></br>
        </div>
    </div>
    <br>
    <script src="https://www.paypal.com/sdk/js?client-id=sb"></script>
    <script>paypal.Buttons({
        createOrder:function(data, actions){
            return actions.order.create({
                purchase_units:[{
                    amount: {
                        value:115
                    }
                }]
            });
        }
    }).render('body');</script>
</body>
</html>